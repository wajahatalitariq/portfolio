"use client";

import CyberLoader from "@/components/ui/CyberLoader";

/**
 * Loading Component
 * 
 * Used automatically by Next.js during Server-Side rendering and 
 * data-fetching phases.
 */
export default function Loading() {
    return <CyberLoader />;
}
